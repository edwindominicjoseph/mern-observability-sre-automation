# Example SSH config for Ansible access
Host mern-app
  HostName <your-ec2-public-ip>    # Replace with the actual EC2 IP address
  User ubuntu                   # Or 'ubuntu' depending on your AMI
  IdentityFile ~/.ssh/Edwin.pem    # Replace with your .pem key filename
  IdentitiesOnly yes
